<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.LocationCacheMapper">
    <select id="findByCoordinates" resultType="com.example.demo.entity.LocationCache">
        SELECT * FROM location_cache WHERE ROUND(longitude, 4) = ROUND(#{longitude}, 4) AND ROUND(latitude, 4) = ROUND(#{latitude}, 4)
    </select>
</mapper>